# tests

| File | What it covers | How to run |
|---|---|---|
| `slot_engine.test.js` | `src/slot_engine.js` — timezones, DST, working hours, `min_hours_notice`, busy overlaps, `preferDate` guards, free/busy response validation | Copy into the n8n container and run with `NODE_PATH` pointed at n8n's `node_modules` (Luxon must be the same one the Code node uses) |
| `prompt_suites.py` | `src/concierge_system_prompt.txt` — inventory assertion, language matching **including booking-with-slots**, never-invent-a-time, (4) facts the row holds are not asked for again, (5) the stated name held against a transcript that says John, (6) a question about another time is not a change request, (7) no promise of a meeting the workflow is not booking | On the server: `python3 tests/prompt_suites.py` (reads `.env`). `N_WITH_SLOTS=8` raises the sample on the booking-with-slots language cases |
| `booking_check.test.js` | `src/booking_check.js` — the clock over the calendar, cancelled/missing retire, unreadable keeps and says so | `node tests/booking_check.test.js`, runs anywhere |
| `booking_claim.test.js` | `src/booking_claim.js` — assertions caught in pt/es/en, offers, questions and negations pass | `node tests/booking_claim.test.js`, runs anywhere |
| `event_id.test.js` | `src/event_id.js` — smallest unused generation, racers agree, foreign ids ignored | `node tests/event_id.test.js`, runs anywhere |
| `transcript.test.js` | `src/transcript.js` — origin fallback, turn labels, hand-back note placement, role alternation; replays the 2026-09-11 re-escalation | `node tests/transcript.test.js`, runs anywhere |
| `budget_range.test.js` | `src/budget_range.js` — the 11 Sep inversion (1.2–1.5M then 1.1M), the mirror case, pass-through, point ranges | `node tests/budget_range.test.js`, runs anywhere |
| `offer_count.test.js` | `src/offer_count.js` — the six identical closings, the lead raising booking restarts the count, colleagues and notes do not count, three languages | `node tests/offer_count.test.js`, runs anywhere |
| `match_confirmation.test.js` | `matchConfirmation()` in `src/slot_engine.js` — the 12 Sep "first meeting" phantom, questions never book, ordinals that point at a slot still do, the rehearsal acceptances verbatim | `node tests/match_confirmation.test.js` (luxon from `cockpit/node_modules`) |
| `booking_stated.test.js` | `src/booking_stated.js` — the documents reply does not state the booking; three languages that do; time alone or weekday alone is not enough | `node tests/booking_stated.test.js`, runs anywhere |
| `reply_name.test.js` | `src/reply_name.js` — the 12 Sep "John" replies, greetings and comma-vocatives in three languages, weekdays/places/agent name never match | `node tests/reply_name.test.js`, runs anywhere |
| `reply_language.test.js` | `src/reply_language.js` — the 12 Sep leak in both directions, the name mask (João), short replies pass, the note and the retry hint | `node tests/reply_language.test.js`, runs anywhere |
| `known_facts.test.js` | `src/known_facts.js` — every fact line, the profile name excluded, budget shapes, empty row renders nothing | `node tests/known_facts.test.js`, runs anywhere |
| `lead_name.test.js` | `src/lead_name.js` — the 11 Sep refusal, profile vs stated, shorter forms, same tokens in another order | `node tests/lead_name.test.js`, runs anywhere |
| `lead_stage.test.js` | `src/lead_stage.js` — retirement regresses the stage, cancel-and-rebook in one turn, `lost` from `not_interested` and revival, nurturing suppression, `lead.qualified` fires once | `node tests/lead_stage.test.js`, runs anywhere |
| `invariants.test.js` | `src/invariants.js` — a DELIBERATELY BROKEN case per invariant, reconstructed from the 11–14 Sep defects (shown-not-stored, "já tem uma reunião marcada", "Hi John", "€1.1M recorded" after rejection, the handoff that failed under a successful notify), plus the passing shape of each once the row holds the fact; the retired notes in three languages are not confirmations; money parsing (M, mil, milhões, €, bare groups; m², minutes, phones and years are not money) | `node tests/invariants.test.js`, runs anywhere. Sabotage-checked 16 Sep: disabling 1 and 4 in a copy turned 7 cases red |
| `ai_disclosure.test.js` | `src/ai_disclosure.js` and invariant 6 — the banner in all three languages and the guards it must not trip (`nameMismatch`, `timesNamedIn`, `moneyAmountsIn`, `bookingClaim`); "assistant" alone is **not** a disclosure, nor is the Portuguese word "ai"; a client override that discloses nothing is rejected for the built-in and resolution never crosses languages; the four reasons (first contact, handback, gap, failsafe) and the `isNewLead` trap; the 1600-character cut sacrifices the reply and keeps the disclosure; **two deliberately broken cases** — required but absent from the text sent, and a path that sends with no disclosure decision recorded | `node tests/ai_disclosure.test.js`, runs anywhere |
| `opt_out.test.js` | `src/opt_out.js` — the three tiers, and the FALSE POSITIVES above all: "não quero perder esta oportunidade", "não quero uma casa com jardim", "não quero nada muito grande, T2 chega" (that last one found a real defect — `nao quero nada` was in the unambiguous phrase list and read a size preference as a permanent objection). Positives in pt/es/en, keywords only as a whole message, accents/emoji/punctuation, a platform block, and the invariant that **nothing permanent is written on a maybe** | `node tests/opt_out.test.js`, runs anywhere |
| *(cockpit)* `reserved-numbers.test.ts`, `suppression.test.ts` | The reserved range `+351900000xxx` and the objection write path. The range exists because the ledger is append-only — a test that writes a real objection cannot clean up — and it is made safe by the send gate **refusing it outright** rather than by a convention. Writes are write-once: caught by running the suite twice, because the first version appended a consent every run and so passed on a virgin database and failed on every run after. Rule 1 is proved here end to end against real rows, alongside the synthetic proof in `consent-resolution.test.ts` | `cd cockpit && npm test` (needs `.env.local`) |
| *(cockpit)* `jurisdiction.test.ts`, `jurisdiction-policy.test.ts` | Stage 1 pieces 3 and 2. The resolver: `+1` is the US, Canada and twenty Caribbean countries and **`+44` is four jurisdictions** — 07911 and 07781 are Guernsey, 07797 is Jersey — plus the four named refusal reasons. The policy evaluation: **an unconfirmed row permits nothing, asserted for every segment**, a missing row and an unconfirmed row stay different facts, a date with no name is not a confirmation, Ireland's twelve-month lapse and the undated consent it cannot age, and the US blocked by the platform ahead of any legal question | `cd cockpit && npm test` |
| *(cockpit)* `gate.test.ts` | `decideGate` — the five layers and, above all, **their order**: a reserved number refused where a confirmed consent and an open policy both say yes; an objection refused where policy permits; an unconfirmed policy row refusing a perfectly good consent; Guernsey resolving to GG and finding no row. Arranged so that the tests which matter are the ones where a LATER layer would have permitted — a gate whose layers are individually right but wrongly ordered passes every test written one layer at a time. Plus §11: the permission states its basis and carries its obligations inside the verdict | `cd cockpit && npm test` |
| *(cockpit)* `one-sender.test.ts`, `dispatch.test.ts` | The send path. **one-sender** is the boundary: at most one file may import the provider SDK, read its credentials, or call its API by URL — it fails with the offending FILENAME, so a person who has never read the design still cannot add a second sender. Proved by sabotage: a `sneaky-sender.ts` importing twilio turned two checks red and named it. **dispatch** is the retry rule: a 4xx is terminal with one attempt, a 429 retries three times at 2s/8s/32s and then halts the batch, and **silence is never retried** — the row stays `intended`, which is a question someone can answer. Plus a forged permit refused because the row is re-read | `cd cockpit && npm test` (needs 0017 applied) |
| *(cockpit)* `evaluate.test.ts` | Phase 1 of a campaign, pure: 300 contacts decided with no database. The shape adds up the way 0018's constraint demands; terminal refusals (objected, unreadable) are separated from provisional ones (an unconfirmed jurisdiction, nothing in the ledger) because only the second kind can flip; a duplicated list is one contact. **The Guernsey case**: +447911123456 with only a GB row present must be refused — the first evaluator picked rows by dialling prefix and would have permitted it under PECR | `cd cockpit && npm test` |
| *(cockpit)* `match.test.ts` | Reconciliation's matcher, pure. **The Concierge reply**: right number, right window, wrong body — must not match, because Twilio offers only To+From+DateSent and the Concierge shares the number, so matching on recipient and time would complete a send row with the provider id of a conversational reply. Exact body only (a trailing newline does not match); two exact matches ALERT rather than resolve; an empty provider listing is a different fact from an empty match; orphan detection for an outbound template with no send row | `cd cockpit && npm test` |
| *(cockpit)* `one-sender.test.ts` — credential split | **Two credentials, two assertions, never one weakened into "at most two files".** The sending credential in at most one file and not the reader; the read credential in at most one file and not the dispatcher; and neither file holding the other's. Plus the reader proved structurally unable to send: no SDK import, exactly one `fetch`, GET hardcoded, no write verb. Credentials are matched **by name anywhere**, not by `process.env.X` — provider-reader defeated the old pattern by accident on its first run, reading them through a helper | `cd cockpit && npm test` |
| *(cockpit)* `reconcile.test.ts` | Both reconciliation passes, with fakes and no network. A found message completes the row with the body and timestamp **from the wire**; a Concierge reply does not complete it; two exact matches alert and never resolve; a row with no body refuses to guess and does not even ask. **The pass never sends** — asserted by reading its own source for any route to dispatch. Orphan sweep halts on a template with no send row, and a clean sweep states what it examined | `cd cockpit && npm test` |
| `docs_guard.sh` | Would committing right now DELETE anything from a doc? Two comparisons: content staged but absent from the file (always a bug — the next plain `git add` reverts it silently), and content in HEAD but absent from the file (the loss as it would land). Written after §9b of the lessons, where four paragraphs vanished inside a commit that described adding something else | `./tests/docs_guard.sh` from anywhere in the repo; optional path arguments. Exits 1 when something would be lost |
| `sabotage_invariants.py` | The live proof for the invariants (§0.7): writes a gitignored copy of the shipping workflow whose two invariant nodes are fed false evidence only when the inbound message carries `SABOTAGE-A` (1, 2, 3, 5, 4), `SABOTAGE-B` (3b) or `SABOTAGE-C` (6 — the run payload claims a disclosure was due and hands the check a `sent_head` with no AI term in it; the lead's own message stays correctly disclosed, only the evidence lies). Deploy, send the marked messages, observe, redeploy the real build | `python3 tests/sabotage_invariants.py`; procedure in the runbook section "The six invariants" |
| `render_slots_block.py` | Not a test — renders the `AVAILABLE_SLOTS` block **out of the shipping node** for the two suites above | Imported by `prompt_suites.py`; run directly to eyeball the block |

**§9.10 is now a deterministic guard, not only a probe.** `ParseClaude` and
`ParseGuardRetry` reject any reply naming a time the workflow did not supply,
routing into the existing guard-retry and escalating if the retry also fails.
The probe suite scored 18/18 while the shipping Concierge invented times,
because every case in it asked about a day the slot list covered — suite 3c
now covers the day it does not.

**Run `prompt_suites.py` after any prompt change.** A prompt edit has now had an
effect outside the section being edited three times — Portuguese examples
changing reply language at B1, and an English slot list doing the same at C1.

The language suite deliberately includes the **booking-with-slots** combination.
It was added because two suites both passed while the gap between them did not
belong to either: the never-invent suite graded times and ignored language, the
language suite had no case carrying a slot list. See `engineering-lessons.md`.

**Neither suite may hold its own copy of a prompt fragment.** `prompt_suites.py`
used to read the slot block from `src/available_slots_block.example.txt`. The
copy drifted one sentence behind the node — the missing sentence being the very
instruction that stops the language leak — so the suite reported a failure that
belonged to the copy, not to the product. It now renders the block from the
workflow via `render_slots_block.py`, which **raises** rather than falling back
if it cannot reach the shipping artefact. That fallback is the defect, not the
safety net. See `engineering-lessons.md` §1, instance 9.

`render_slots_block.py` shells out to `node` **inside the n8n container**,
because the block is built by JavaScript in `BuildClaudeRequest` and the host
has no node. Python only slices the fragment between two fixed markers; node
does all the semantics.
